// Selectors

// Modules

const gameBoard = () => {
  const board = ["", "", "", "", "", "", "", "", ""];
};

const displayController = () => {
  const grid = document.querySelectorAll(".grid");
};

// Factories

// flowCtrl obj

// displayController module obj
